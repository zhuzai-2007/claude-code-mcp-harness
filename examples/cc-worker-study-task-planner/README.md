# Study Task Planner Example

Small Python CLI example produced during worker smoke testing.

## Usage

```powershell
python .\study_task_planner.py .\tasks.example.json
python .\run_tests.py
```

The tool reads a JSON file with a `tasks` array and writes sorted tasks to stdout.
