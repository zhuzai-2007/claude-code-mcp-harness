# Supervisor v1.10.0-beta.1 release candidate

Status: **release candidate — pending ChatGPT Web validation**

This document records the source baseline and non-paid validation evidence for the v1.10.0-beta.1 candidate. It does not declare the Public Beta ready.

## Baseline

- Branch: `release/v1.10.0-beta.1`
- Implementation baseline commit: `b825bb7888e1a2db7af51c8293cd8e2c8c937a3e`
- Certification commit: `5f67c5203a26e3433bd37cf432a927f865a26a44`
- Clean clone reproducibility fix: `a4847e636b58be6fda913a0c3dedb06b5613733f`
- Audited code/test candidate commit: `a4847e636b58be6fda913a0c3dedb06b5613733f`
- Public repository: `https://github.com/zhuzai-2007/claude-code-mcp-harness`
- Version: `1.10.0-beta.1`
- Readiness record: `pending_gpt_web_validation`

## Included scope

- Project-first Dashboard;
- release/local/runtime Project Registry layering;
- Planner Resource Selection;
- read-only Project Context Snapshot;
- Dashboard Settings with immutable code-owned limits;
- automatic Node test discovery;
- clean onboarding validation.

This release does not change the Task Runtime state machine, Workflow Runtime semantics, Harness audit contract, approval boundary, side-effect guard, or existing MCP tool semantics.

## Supported environment

- Windows PowerShell;
- Node.js 20 or newer with npm;
- Claude Code CLI configured for a compatible provider;
- loopback local Bridge and dependency-free Dashboard;
- OpenAI `tunnel-client` for supported ChatGPT custom-MCP access.

ChatGPT plan, developer-mode, custom-app, write-action, Platform Tunnel, and workspace-association availability are controlled by OpenAI and can change independently of this repository. At the time of this release candidate, OpenAI documents full MCP write actions for supported Business and Enterprise/Edu workspaces. Confirm current availability in the official documentation before onboarding.

## Architecture overview

```text
ChatGPT Web
  -> OpenAI Secure MCP Tunnel
  -> local MCP Bridge
  -> Supervisor Decision / Workflow orchestration
  -> Task Runtime
  -> Harness policy, resource, side-effect, and audit controls
  -> Claude Code Worker
  -> observed evidence and review package
```

See [Supervisor Architecture](../ARCHITECTURE.md) for layer responsibilities and stable boundaries.

## Validation status

Validated against the candidate commit chain above, ending at the clean clone reproducibility fix:

| Gate | Result |
| --- | --- |
| Node tests | PASS — 30/30 automatically discovered test files in the developer checkout and isolated clean clone |
| Harness smoke | PASS |
| MCP mock protocol | PASS — mock Workflow completed; no real Plan or write call |
| Clean clone validation | PASS — no local registry, Runtime data, dependencies, config, or historical runs were present before install; install, Doctor, start check, and 30/30 tests passed |
| Release baseline | PASS — clean Git baseline without skipping the Git cleanliness gate |
| Registry validation | PASS — four release Projects materialized; an absent local registry is supported |
| Secret scan | PASS — no secret-shaped credential or private-key match in tracked release files |
| Documentation hygiene | PASS — documentation hygiene and local-link checks passed |
| Cached/worktree whitespace checks | PASS |
| Tracked local path, private URL, config, and runtime-data scan | PASS |
| Beginner onboarding contract | PASS |
| Fresh-session ChatGPT Web Plan → Approval → Run → Review | PENDING |

The mock protocol uses no real Plan or write call. Real Provider and ChatGPT Web validation are intentionally separate because they can be billable and require operator-controlled credentials.

## Known limitations

- Windows is the primary validated platform.
- Execution stages are sequential; there is no parallel Agent execution.
- The Dashboard uses polling and has no notification service.
- Approval names are local audit metadata, not authenticated identities.
- Runtime and evidence persistence are file-backed, not tamper-proof.
- Provider cost summaries may differ from upstream billing.
- The local Bridge must remain on loopback; ChatGPT access depends on a correctly configured Secure MCP Tunnel.
- ChatGPT custom-MCP and write-action availability depends on account, workspace, admin, and Platform Tunnel permissions.
- Worker ownership observability and automatic process recovery are not part of this release.
- One transient Windows file-lock/timing failure was observed during repeated local Workflow Runtime testing; the isolated clean clone and final full 30/30 test gates passed.

## Remaining release gates

Before creating a tag:

1. run the documented workflow from a new ChatGPT Web conversation;
2. record the Supervisor Review outcome without committing Workflow ids, local paths, Tunnel URLs, credentials, or Runtime data;
3. decide whether the Git author identity in commit metadata is intended for public disclosure;
4. update release readiness only after those checks pass.

See [Getting started from zero](../getting-started-from-zero.md) for operator onboarding and [Using Supervisor from ChatGPT Web](../gpt-web-usage.md) for the manual acceptance sequence.
